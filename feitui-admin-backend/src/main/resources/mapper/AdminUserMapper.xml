<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feitui.admin.mapper.AdminUserMapper">

    <!-- 登录查询需含密码 -->
    <select id="findByUsername" resultType="com.feitui.admin.entity.AdminUser">
        SELECT id, username, password, nickname, role, status, last_login_time, create_time, update_time
        FROM t_admin_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="findById" resultType="com.feitui.admin.entity.AdminUser">
        SELECT id, username, password, nickname, role, status, last_login_time, create_time, update_time
        FROM t_admin_user WHERE id = #{id}
    </select>

    <!-- 列表查询不返回密码 -->
    <select id="page" resultType="com.feitui.admin.entity.AdminUser">
        SELECT id, username, nickname, role, status, last_login_time, create_time, update_time
        FROM t_admin_user
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%') OR nickname LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*) FROM t_admin_user WHERE username = #{username}
    </select>

    <select id="selectIds" resultType="java.lang.Integer">
        SELECT id FROM t_admin_user ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="com.feitui.admin.entity.AdminUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_admin_user (id, username, password, nickname, role, status)
        VALUES (#{id}, #{username}, #{password}, #{nickname}, #{role}, #{status})
    </insert>

    <update id="updateInfo">
        UPDATE t_admin_user
        SET nickname = #{nickname}, role = #{role}, status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE t_admin_user SET password = #{password} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_admin_user WHERE id = #{id}
    </delete>

    <update id="shiftIdDown">
        UPDATE t_admin_user SET id = id - 1 WHERE id > #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE t_admin_user SET last_login_time = NOW() WHERE id = #{id}
    </update>

</mapper>