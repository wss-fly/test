<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feitui.mapper.VideoMapper">

    <resultMap id="VideoMap" type="com.feitui.entity.Video">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="video_url" property="videoUrl"/>
        <result column="cover_image" property="coverImage"/>
        <result column="duration" property="duration"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="VideoMap">
        SELECT * FROM t_video WHERE status = 1 ORDER BY sort ASC
    </select>

    <select id="selectAllForAdmin" resultMap="VideoMap">
        SELECT * FROM t_video ORDER BY sort ASC, id ASC
    </select>

    <select id="selectById" resultMap="VideoMap">
        SELECT * FROM t_video WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.feitui.entity.Video">
        INSERT INTO t_video (id, title, description, video_url, cover_image, duration, sort, status)
        VALUES (#{id}, #{title}, #{description}, #{videoUrl}, #{coverImage}, #{duration}, #{sort}, #{status})
    </insert>

    <update id="updateById" parameterType="com.feitui.entity.Video">
        UPDATE t_video
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="videoUrl != null">video_url = #{videoUrl},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_video WHERE id = #{id}
    </delete>

    <select id="selectIds" resultType="java.lang.Integer">
        SELECT id FROM t_video ORDER BY id ASC
    </select>

    <update id="shiftIdDown">
        UPDATE t_video SET id = id - 1 WHERE id > #{id}
    </update>

    <select id="selectSortById" resultType="java.lang.Integer">
        SELECT sort FROM t_video WHERE id = #{id}
    </select>

    <update id="updateSort">
        UPDATE t_video SET sort = #{sort} WHERE id = #{id}
    </update>

    <update id="bumpId">
        UPDATE t_video SET id = id + #{big} WHERE id IN (#{a}, #{b})
    </update>

    <update id="setId">
        UPDATE t_video SET id = #{to} WHERE id = #{from}
    </update>

    <select id="countBySort" resultType="int">
        SELECT COUNT(*) FROM t_video WHERE sort = #{sort}
    </select>

    <select id="countBySortExcept" resultType="int">
        SELECT COUNT(*) FROM t_video WHERE sort = #{sort} AND id != #{id}
    </select>
</mapper>
